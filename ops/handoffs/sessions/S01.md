# S01 — session handoff

> Date: 2026-07-12. One log per session. The lean master (`ops/handoffs/HANDOFF.md`) keeps only the CURRENT
> block + queue + locked decisions + persistent reference; this file is the durable per-session record.
> (First session under the agentic kit; earlier product sessions predate this log and live in
> `../../../../HANDOFF.md` history — the workspace master in the parent directory.)

## Shipped
- PR #3 (P1-2/6/7 server hardening) squash-merged to main (`0ac189a`).
- Agentic kit committed to main as its own commit (`e4d23b6`, 278 files) after the SETUP.md interview
  filled `company.yml` with CUT's facts (owner Zarif, is_technical false, App Store subscription, price TBD).
- `kb/icp-research.md` — sourced ICP research, 23 public sources; beachhead = the repeat cutter.
- `kb/message-spine.md` — canonical story: decision-not-data / built-for-the-bad-day / keep-the-muscle.
- CEO Twin bootstrapped: `kb/owner-profile.md`, `ops/decisions/ledger.jsonl`, `ops/briefs/2026-07-12-ceo-twin-brief.md`.
- PR #4 (hardening polish: middleware-order integration test, clerk limiter tests, shutdown error branches,
  PG_POOL_MAX clamp; Knox 90) merged on Zarif's tap (`1d8dcea`).
- PR #5 (P1-11 native lane: bundle id com.zarifahmed.cut + android.package, eas.json dev/preview/prod,
  expo-dev-client ~6.0.21) merged on Zarif's tap (`78d1d11`).
- PR #6 (P1-8: x-forwarded-host allowlist shared with CORS, env-key fallback, latent host-less 500 fixed,
  Clerk-Proxy-Url scheme pinned https; Knox 88, Cyrus SHIP) merged on Zarif's tap (`488b658`).
- Tests 56 → 82 across the day; suite verified green on main after every merge.
- mcporter Exa registration persisted (free hosted search, no key).

## Deployed + verified
- No deploy this session (no live surface exists yet; product ships via App Store later).

## Open actions (owner)
- Prune stale remote branch `p1-2-6-7-server-hardening` in the GitHub UI (agent permission-blocked).
- Someday: `eas init` with Zarif's Expo account (EAS projectId/owner), actual EAS builds — cost-gated.
- Replit simulator QA — deferred again by Zarif this session; still the top open risk.

## Decisions locked (do not relitigate)
- iOS bundle identifier = **com.zarifahmed.cut** (ledger `bundle-id-com-zarifahmed-cut-4`; locks at first submission).
- Cost rule: anything increasing cost basis (Replit, paid APIs, usage) is asked BEFORE running — every time.
- Twin-authored PRs are never self-merged (CLAUDE.md Lessons; owner taps merge).
- owner.is_technical = false → plain English, starred recommendations, no jargon.
- Replit QA = "not yet" (ledger `replit-simulator-qa-2-resolved`, decline — re-raise, don't assume).

## Commits
- Main advanced `4dd4516` → `5466cbb`+ this handoff commit: PR merges #3–#6 plus kit/kb/ops commits
  (`e4d23b6`, `579faf0`, `9ef61b1`, `5466cbb`, mcporter config).

## Next step / resume point
- **P1-9/10** (zod ^4 catalog migration + `codegen:check` drift gate) — audit §P1-9/§P1-10; owner said
  "keep going" then chose a fresh session instead; ledger kickoffs `-8`/`-9` rescinded → parked here.
  Watch: orval/zod-v3 `format: uuid` ban in `.agents/memory/orval-zod-codegen.md` (retire it as part of P1-9);
  `minimumReleaseAge` gate on the zod bump; run `pnpm --filter @workspace/api-spec run codegen` after
  any openapi.yaml change.
- Then **P1-4** (atomic onboarding: set flag inside profile-upsert transaction server-side, reject flag-only
  PATCH without profile, document invariant — audit §P1-4). No dependency changes → can run in a parallel
  WORKTREE (use worktree isolation; two agents sharing the main checkout collided this session).

## Deferred shortcuts
- P1-11's EXPO_PUBLIC_* → app.config.ts / per-profile EAS env migration (audit calls it "rest before
  purchase QA") — deliberately out of P1-11's scope; pay back before RevenueCat purchase QA.
- Cyrus residual for the Replit QA pass: confirm the edge REPLACES (not appends) x-forwarded-proto/host;
  also verify `trust proxy = 1` hop count.
- In-memory rate-limit store (per-instance) + Clerk `user.updated` email-refresh webhook — deferred since PR #3.

## Queue deltas
- Removed (done): PR #3 merge, kit commit/move, ICP research, message spine, PR-#3 review follow-ups (all 4),
  P1-11, P1-8.
- Added: P1-9/10 + P1-4 as the parked next batch; EXPO_PUBLIC_* migration payback item; Cyrus edge-header
  check folded into the Replit QA prompt.
