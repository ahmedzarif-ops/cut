{
  "name": "workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": "24.x",
    "pnpm": "10.34.5"
  },
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run codegen:check && pnpm run typecheck && pnpm run test && pnpm -r --if-present run build",
    "codegen": "pnpm --filter @workspace/api-spec run codegen",
    "codegen:check": "node ops/scripts/codegen-check.mjs",
    "validate:app-store": "node scripts/app-store/validate.mjs",
    "validate:app-store:release": "node scripts/app-store/validate.mjs --release",
    "test:app-store": "node --test scripts/app-store/validate.test.mjs",
    "verify:deploy": "node ops/scripts/deploy-verify.mjs",
    "verify:post-build-evidence": "node ops/scripts/post-build-evidence-verify.mjs",
    "test:release-ops": "node --test ops/scripts/deploy-verify.test.mjs ops/scripts/post-build-evidence-verify.test.mjs ops/scripts/production-migration-policy.test.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "pnpm run test:release-ops && pnpm run test:app-store && pnpm -r --if-present run test"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "eas-cli": "21.4.0",
    "prettier": "^3.9.4",
    "typescript": "~5.9.3"
  }
}
