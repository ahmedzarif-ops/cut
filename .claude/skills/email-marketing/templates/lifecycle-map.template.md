# Lifecycle Map

The `email-lifecycle-strategist` fills this from the brief + the KBs. It is the plan the owner
approves at the MAP GATE and the input the `email-copywriter` builds from. No send-ready copy here,
just the plan. End with the honesty separation block.

## 1. Executive read
<3 to 5 sentences: the situation, the recommended lifecycle push, and the single biggest bet.>

## 2. Lifecycle map (the ladder as flows)
<the offer ladder rendered as the connected flows: which programs move a subscriber from each
funnel stage to the next; entry points and the spine path.>

## 3. Audience and segmentation
<which segments this run targets (funnel-stage / segment / buyer-reality / primary-leak /
engagement), and the message shift per segment.>

## 4. Contact-property and event schema
<the platform-neutral fields + events this run relies on (from data-and-deliverability.md), and
any new ones.>

## 5. Editorial-calendar framework
<how the pillars rotate for any recurring sends in scope; not a dated calendar instance.>

## 6. Per-program specs (one per in-scope program)
For each program (A-L) in scope, define: audience; entry trigger; eligibility; exclusions; exit
criteria; re-entry rules; wait times; branch logic; primary CTA; frequency-cap interaction;
owner-handoff conditions; measurement plan; email count.

## 7. Framework selector instance
<which framework each sequence/email uses (from frameworks-and-research.md) and why.>

## 8. Measurement + experimentation plan
<the primary + guardrail metrics for this run (from measurement.md) and the first 2 to 4 tests,
each as a one-line hypothesis at the right level. What is and is not instrumented today.>

## 9. 90-day roadmap
<the prioritized order to build the remaining programs over 90 days.>

## 10. Ranked first-20 emails
For each (highest value first): segment, trigger, goal, core lesson, CTA, success metric.
1.
2.
...

## 11. Honesty separation
- Observed facts about the company:
- Evidence-supported strategy:
- Hypotheses:
- Recommendations requiring testing:
- Claims requiring primary research or customer validation:
