# Email Sequence (the copywriter's contract)

The exact shape `email-copywriter` produces for each in-scope program. One file per program in the
run's `sequences/` folder. Paste-ready (drop into the ESP), voice-clean per guardrails,
message-matched to the destination surface.

## Sequence header

- Program: <A-L + name>
- Audience / segment:
- Entry trigger:
- Exit criteria:
- Frequency-cap interaction:
- Owner-handoff condition (if any):
- Primary success metric:

## Per email (repeat for each email in the sequence)

### Email <n>: <internal name>

- Factor category (from message-factor-model.md):
- Pillar (which message-spine pillar):
- Lifecycle stage:
- Email type: education / activation / conversion / retention / reactivation
- Framework used:
- Wait / send timing (from entry or prior email):
- Subject line:
- Preheader: (adds information, does not restate the subject)
- Body:
  <the full ready-to-send body. Plain, audience-aware, honoring brand.voice and brand.banned_phrases.
  One useful lesson. Teach before asking. Honest proof only. Live pricing (from config) if mentioned.>
- Primary CTA: (one)
- Why this CTA follows the value:
- Personalization fields used (with a fallback for each):
