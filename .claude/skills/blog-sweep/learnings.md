# Blog Sweep Learnings (reserved log)

Reserved learning log for the recurring anti-slop sweep skill (`blog-sweep`).
The sweep appends its per-run results to the `blog-engine` learnings log (the shared
published-registry and what-works memory). This file is the sweep's own reserved slot:
if a sweep-specific pattern or guardrail adjustment needs a dedicated note separate from
the writer's registry, it goes here. Start each entry with the ISO date and sweep run
number.

## Sweep run log
(Entries appended after each run, newest first.)

## Recurring slop patterns flagged by the sweep
(Promoted here only after HUMAN review; never silently edited by the sweep itself.)
